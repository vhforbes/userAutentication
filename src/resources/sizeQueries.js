const device = {
  mobile: '(max-width: 425px)',
  desktopS: '(max-width: 1024px)',
  desktop: '(max-width: 2560px)',
};

// Usage example:
// @media ${device.mobile} { css here! }

export default device;
